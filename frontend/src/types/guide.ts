export interface Guide {
  id: string;
  name: string;
  avatar: string;
  bio: string;
  languages: string[];
  rating: number;
  reviewCount: number;
  tourCount: number;
  certifications: string[];
  joinedDate: string;
}
